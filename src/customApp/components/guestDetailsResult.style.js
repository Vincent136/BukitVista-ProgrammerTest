import styled from 'styled-components';
import withDirection from '../../settings/withDirection';

const GuestDetailStyleWrapper = styled.div`
    width: 100%;
    display: flex;
    text-align: left;
    align-items: left;
    padding: 2rem;
`;

export default withDirection(GuestDetailStyleWrapper)